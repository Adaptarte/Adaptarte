import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  img: {
    aspectRatio: 1,
    height: undefined,
    resizeMode: "contain",
    width: undefined
  }
});

export { styles };
