import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  checkBox: {
    alignSelf: "flex-end"
  },
  container: {
    borderRadius: 10,
    display: "flex",
    flexDirection: "column",
    padding: 8
  },
  img: {
    aspectRatio: 1.5,
    marginBottom: 8,
    width: "100%"
  }
});

export { styles };
