import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  box: {
    alignItems: "center",
    borderRadius: 8,
    borderWidth: 3,
    display: "flex",
    height: 24,
    justifyContent: "center",
    width: 24,
  },
});

export { styles };
