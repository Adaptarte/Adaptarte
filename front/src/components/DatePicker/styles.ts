import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "stretch",
    display: "flex",
    flexDirection: "column",
    paddingVertical: 4
  },
  label: {
    marginBottom: 4
  },
  picker: {
    height: 120,
    width: undefined
  }
});

export { styles };
