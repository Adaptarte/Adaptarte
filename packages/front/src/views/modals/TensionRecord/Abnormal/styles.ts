import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  center: {
    alignItems: "center",
    display: "flex",
    flexDirection: "column"
  },
  message: {
    marginVertical: 8
  }
});

export { styles };
