import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  plusButton: {
    alignItems: "center",
    aspectRatio: 1,
    display: "flex",
    justifyContent: "center",
  },
});

export { styles };
