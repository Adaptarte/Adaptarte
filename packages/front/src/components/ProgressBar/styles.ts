import { StyleSheet } from "react-native";

import { colors } from "styles/colors";

const styles = StyleSheet.create({
  bar: {
    backgroundColor: colors.BLUE_PURPLE,
    borderRadius: 4,
    height: 20,
  },
  container: {
    backgroundColor: colors.BLUE_TRANSLUCID,
    borderRadius: 4,
    height: 20,
  },
});

export { styles };
