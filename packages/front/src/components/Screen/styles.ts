import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    minHeight: "100%",
    padding: 16,
  },
  root: {
    height: "100%",
    width: "100%",
  },
});

export { styles };
